# Optional exact frontend origin. Use a specific origin in production.
CORS_ORIGIN=http://localhost:5173

# Optional comma-separated region allowlist, e.g. US,CA,GB
ALLOWED_COUNTRIES=

# Add provider credentials only for documented, authorized integrations.
# PROVIDER_API_KEY=
